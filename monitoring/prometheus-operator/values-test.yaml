prometheus:
  enabled: false

# Alertmanager 비활성화
alertmanager:
  enabled: false

# Grafana 비활성화
grafana:
  enabled: false

# 기타 메트릭 수집기 비활성화
kube-state-metrics:
  enabled: false
prometheus-node-exporter:
  enabled: false

# PrometheusRule 리소스 생성을 막는 핵심 설정
defaultRules:
  create: false